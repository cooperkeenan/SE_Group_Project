<!-- Views/WeatherPage.xaml -->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:EnviroMonitorApp.Models"
             x:Class="EnviroMonitorApp.Views.WeatherPage"
             Title="Weather">

    <CollectionView ItemsSource="{Binding Forecast}"
                    Margin="8">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:WeatherRecord">
                <Frame Padding="10" Margin="4" CornerRadius="8" BorderColor="LightGray">
                    <HorizontalStackLayout Spacing="12">
                        <Label Text="{Binding Timestamp,
                                              StringFormat='{}{0:dd MMM HH:mm}'}"
                               FontAttributes="Bold"/>
                        <Label Text="🌡️"/>
                        <Label Text="{Binding Temperature, StringFormat='{}{0:F1} °C'}"/>
                        <Label Text="💧"/>
                        <Label Text="{Binding Humidity, StringFormat='{}{0}%'}"/>
                        <Label Text="🌬️"/>
                        <Label Text="{Binding WindSpeed, StringFormat='{}{0:F1} m/s'}"/>
                    </HorizontalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
